import { describe, test, expect } from 'vitest'

describe('Workout page component with generated workout', () => {
  test('should render the generated workout', () => {
    expect(true).toBe(true)
  })
})
